# ディスク容量クリーンアップガイド

**作成日**: 2025-01  
**目的**: Flutterインストールのためにディスク容量を確保

---

## 🎯 目標

- **空き容量**: 5GB以上を確保
- **現在**: 約3.1GB
- **必要**: あと約2GB以上

---

## 🧹 安全なクリーンアップ手順

### Step 1: Homebrewのクリーンアップ（安全）

```bash
# 未使用のパッケージとキャッシュを削除
brew cleanup --prune=all

# 古いバージョンのパッケージを削除
brew autoremove
```

**期待される削減**: 500MB〜2GB

---

### Step 2: システムキャッシュの削除（安全）

```bash
# ユーザーキャッシュを削除
rm -rf ~/Library/Caches/*

# 一時ファイルを削除
rm -rf /tmp/*

# システムログの古いファイルを削除（管理者権限不要）
sudo rm -rf /private/var/log/asl/*.asl
sudo rm -rf /private/var/log/system.log.*
```

**期待される削減**: 100MB〜500MB

---

### Step 3: Xcodeのクリーンアップ（Xcodeを使用している場合）

```bash
# 派生データを削除
rm -rf ~/Library/Developer/Xcode/DerivedData/*

# 古いアーカイブを削除
rm -rf ~/Library/Developer/Xcode/Archives/*

# シミュレーターのキャッシュを削除
xcrun simctl delete unavailable
```

**期待される削減**: 500MB〜5GB（使用状況による）

---

### Step 4: ダウンロードフォルダの確認

```bash
# 大きなファイルを確認
du -sh ~/Downloads/* 2>/dev/null | sort -hr | head -10
```

**削除対象**:
- インストール済みの.dmgファイル
- 古いインストーラー
- 既に使用済みのzipファイル

**注意**: 削除前にファイルの内容を確認してください

---

### Step 5: Dockerのクリーンアップ（Dockerを使用している場合）

```bash
# 未使用のコンテナ、イメージ、ボリュームを削除
docker system prune -a --volumes

# 確認してから削除
docker system df
```

**期待される削減**: 1GB〜10GB（使用状況による）

---

### Step 6: 大きなファイルを検索

```bash
# 100MB以上のファイルを検索
find ~ -type f -size +100M -exec ls -lh {} \; 2>/dev/null | sort -k5 -hr | head -20
```

**確認して削除できるもの**:
- 大きな動画ファイル（既にバックアップ済み）
- 古いバックアップファイル
- 大きなログファイル

---

## 📊 容量確認

各ステップの後に容量を確認：

```bash
df -h /
```

**目標**: 空き容量が5GB以上になるまで繰り返す

---

## 🔍 詳細な容量分析

### 各ディレクトリの使用量を確認

```bash
# ホームディレクトリ配下の各フォルダのサイズ
du -sh ~/* 2>/dev/null | sort -hr | head -20

# Libraryフォルダの詳細
du -sh ~/Library/* 2>/dev/null | sort -hr | head -20

# Applicationsフォルダのサイズ
du -sh /Applications/* 2>/dev/null | sort -hr | head -20
```

---

## ⚠️ 削除してはいけないもの

- **重要なドキュメント**
- **写真・動画**（バックアップ前に削除しない）
- **アプリケーションの設定ファイル**
- **プロジェクトファイル**（TailorCloud等）

---

## ✅ クリーンアップ後の確認

### 1. ディスク容量を確認

```bash
df -h /
```

### 2. Flutterをインストール

```bash
# 空き容量が5GB以上あることを確認してから
brew install --cask flutter
```

---

## 💡 継続的な容量管理

### 定期的なクリーンアップ

```bash
# 週次で実行することを推奨
brew cleanup
rm -rf ~/Library/Caches/*
```

### 大きなファイルの定期的な確認

```bash
# 月次で実行
du -sh ~/* 2>/dev/null | sort -hr | head -20
```

---

**最終更新日**: 2025-01

