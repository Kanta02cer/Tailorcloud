# TailorCloud UIデザイン仕様書

**作成日**: 2025-01  
**デザインバージョン**: 1.0.0  
**プラットフォーム**: iOS/Android (Flutter)

---

## 🎨 テーマ

**"Professional & Clean"** - プロフェッショナルで清潔感のあるデザイン

---

## 1. グローバルコンポーネント

### 1.1 カラーパレット

| 用途 | カラーコード | 説明 |
|------|-------------|------|
| Primary (Navy) | `#000080` | ボタン、アクティブ状態、ヘッダー |
| Secondary (Text Black) | `#111827` | メイン見出し |
| Accent (Success Green) | `#10B981` | "在庫あり"、"成長率" |
| Accent (Warning Yellow) | `#FBBF24` | "在庫残りわずか"、"コンプライアンス警告" |
| Accent (Danger Red) | `#EF4444` | "売り切れ"、"緊急" |
| Surface (White) | `#FFFFFF` | カード、モーダル |
| Background | `#F8FAFC` | 背景色（ライトグレー） |
| Border/Stroke | `#E5E7EB` | 境界線 |
| Text Gray | `#4B5563` | 本文テキスト |
| Text Dark Gray | `#374151` | カードタイトル |

### 1.2 タイポグラフィ（モバイル）

| 要素 | サイズ | ウェイト | カラー |
|------|--------|---------|--------|
| H1 (ヘッダー) | 18px | Bold | `#111827` |
| H2 (カードタイトル) | 14px | Bold | `#374151` |
| Body | 12px | Regular | `#4B5563` |
| KPI数値 | 28px | Bold | `#111827` |
| ラベル | 10px | Regular | Gray |

**フォント**: Noto Sans JP (Medium, Bold)

### 1.3 フローティングアクションボタン (FAB)

- **形状**: 円形 (56x56px)
- **カラー**: Primary Navy `#000080`
- **アイコン**: プラスアイコン (White, 24px)
- **シャドウ**: `0px 4px 12px rgba(0, 0, 0, 0.25)`
- **位置**: 固定、右下（タブバーの上）

---

## 2. 画面設計

### 2.1 Screen 1: Home (Dashboard)

#### A. ヘッダーエリア

**レイアウト**: Flex Row (Space Between)

```
┌────────────────────────────────────────┐
│ おはようございます、[User Name]様  🔔  │
│                          [Badge: 3]    │
└────────────────────────────────────────┘
```

- **テキスト**: "おはようございます、[User Name]様"
- **アイコン**: ベルアイコン + 赤いバッジ（通知数）

#### B. KPIカード（横スクロール）

**コンテナ**: 140x100px、白、角丸12px、ストローク `#E5E7EB`

```
┌──────────────┐
│ 今月の売上    │
│              │
│ ¥2,450,000   │
│              │
│ ↑ +15%       │
└──────────────┘
```

- **ラベル**: "今月の売上" (10px, Gray)
- **値**: "¥2,450,000" (20px, Bold)
- **トレンド**: "↑ +15%" (10px, Green)

**KPI項目**:
- 今月の売上
- 今月の注文件数
- 承認待ちタスク数
- 今月の成長率

#### C. タスクリスト（"The Hook"）

**コンテナ**: 白、角丸12px、パディング16px

```
┌────────────────────────────────────────┐
│ 承認・確認が必要なタスク  [Badge: 3]   │
├────────────────────────────────────────┤
│ ⚠️  下請法対応書類の承認待ち   [確認]  │
│ ⚠️  生地在庫の確認が必要         [確認] │
│ 📋  新規注文の承認待ち           [確認] │
└────────────────────────────────────────┘
```

- **ヘッダー**: "承認・確認が必要なタスク" (Badge: 3)
- **行アイテム**:
  - アイコン: 警告トライアングル（黄色背景）
  - テキスト: "下請法対応書類の承認待ち" (Bold)
  - アクション: 小さな"確認"ボタン（アウトライン）

**タスクタイプ**:
- 下請法対応書類の承認待ち
- 生地在庫の確認が必要
- 新規注文の承認待ち
- 工場からの返信待ち

---

### 2.2 Screen 2: Inventory (Stock Check)

#### A. 検索バー

**入力欄**: 高さ44px、角丸8px、背景 `#F3F4F6`

```
┌────────────────────────────────────────┐
│ 🔍  生地を検索...           📷 QR      │
└────────────────────────────────────────┘
```

- **アイコン左**: 検索（グレー）
- **アイコン右**: QRコードスキャン（黒）← **強調！**

#### B. フィルタータブ

**スタイル**: テキストリンク、アクティブ時は下線

```
[すべて]  [在庫あり]  [残りわずか]
  ────
```

**アイテム**:
- すべて
- 在庫あり
- 残りわずか

#### C. 生地グリッド

**レイアウト**: 2列グリッド

**カード**: アスペクト比 1:1 画像 + 下に詳細

```
┌────────┐  ┌────────┐
│  [画像] │  │  [画像] │
│   ◎    │  │ ⚠2.5m  │
└────────┘  └────────┘
│ 生地名  │  │ 生地名  │
│ ¥4,500 │  │ ¥4,500 │
└────────┘  └────────┘
```

**ステータスバッジ**（画像右上にオーバーレイ）:

1. **在庫あり**: 
   - 緑色の円アイコン "◎"
   - Stock > 3.2m

2. **在庫残りわずか**: 
   - 黄色のトライアングル "⚠ 残り2.5m"
   - テキストサイズ: 10px
   - 3.2m > Stock > 0

3. **売り切れ**: 
   - 赤いバッジ "SOLD OUT"
   - 画像の不透明度 50%
   - Stock = 0

#### D. アクションモーダル（ボトムシート）

**トリガー**: "在庫残りわずか"アイテムをタップ

**コンテンツ**:

```
┌────────────────────────────────────────┐
│                                       ×│
│                                         │
│  生地名: Premium Navy Wool              │
│  価格: ¥4,500/m                         │
│                                         │
│  在庫ステータス:                        │
│  ⚠️  残り 2.5m (スーツ1着分)            │
│                                         │
│  [この生地を確保して発注]                │
│  (Full Width, Primary Color)           │
└────────────────────────────────────────┘
```

- 生地名 & 価格
- **在庫ステータス**: "残り 2.5m (スーツ1着分)" (Bold, Yellow)
- **ボタン**: "この生地を確保して発注" (Full Width, Primary Color)

---

## 3. バックエンドAPI連携要件

### 3.1 Home Dashboard API

#### GET /api/dashboard?tenant_id={tenant_id}

**レスポンス例**:
```json
{
  "kpis": {
    "monthly_revenue": {
      "value": 2450000,
      "trend": "+15",
      "trend_direction": "up"
    },
    "monthly_orders": {
      "value": 42,
      "trend": "+8",
      "trend_direction": "up"
    }
  },
  "pending_tasks": [
    {
      "id": "task-1",
      "type": "compliance_approval",
      "title": "下請法対応書類の承認待ち",
      "icon": "warning",
      "count": 3
    }
  ]
}
```

### 3.2 Inventory API

#### GET /api/fabrics?tenant_id={tenant_id}&status={status}

**クエリパラメータ**:
- `tenant_id`: テナントID（必須）
- `status`: フィルター（`all`, `available`, `limited`, `soldout`）
- `search`: 検索キーワード（オプション）

**レスポンス例**:
```json
{
  "fabrics": [
    {
      "id": "fabric-1",
      "name": "Premium Navy Wool",
      "supplier_id": "supplier-1",
      "price": 4500,
      "stock_amount": 5.2,
      "stock_status": "Available",
      "image_url": "https://...",
      "minimum_order": 3.2
    },
    {
      "id": "fabric-2",
      "name": "Classic Black",
      "price": 4200,
      "stock_amount": 2.5,
      "stock_status": "Limited",
      "image_url": "https://...",
      "minimum_order": 3.2
    }
  ]
}
```

#### POST /api/fabrics/{fabric_id}/reserve

**リクエスト**:
```json
{
  "tenant_id": "tenant-123",
  "amount": 3.2
}
```

**用途**: 在庫確保（発注フロー開始）

---

## 4. データモデル要件

### 4.1 Dashboard KPIデータ

バックエンドで集計が必要:
- 月間売上（注文の合計金額）
- 月間注文件数
- 承認待ちタスク数（コンプライアンス、在庫確認等）

### 4.2 Fabric（生地）データ拡張

既存のFabricモデルに追加が必要:
- `image_url`: 生地画像のURL
- `minimum_order`: 最小発注数量（スーツ1着分 = 3.2m）

### 4.3 Task（タスク）データモデル

新規実装が必要:
```go
type Task struct {
    ID          string    `json:"id"`
    TenantID    string    `json:"tenant_id"`
    Type        TaskType  `json:"type"`
    Title       string    `json:"title"`
    Description string    `json:"description"`
    Status      TaskStatus `json:"status"`
    Priority    int       `json:"priority"`
    CreatedAt   time.Time `json:"created_at"`
}
```

---

## 5. 実装優先順位

### Phase 1: MVP UI

1. ✅ カラーパレット定義
2. ✅ タイポグラフィ定義
3. [ ] Home Dashboard画面（KPIカード、タスクリスト）
4. [ ] Inventory画面（検索、フィルター、グリッド）

### Phase 2: 在庫連携

1. [ ] Inventory API実装
2. [ ] 在庫ステータス計算ロジック
3. [ ] QRコードスキャン機能

### Phase 3: 高度な機能

1. [ ] タスク管理システム
2. [ ] ダッシュボードKPI集計
3. [ ] リアルタイム在庫更新

---

## 6. バックエンドAPI拡張が必要な項目

### 6.1 Dashboard API（新規実装）

- [ ] `GET /api/dashboard?tenant_id={id}` - ダッシュボードデータ取得
- [ ] KPI集計サービス
- [ ] タスク管理サービス

### 6.2 Inventory API拡張

- [ ] `GET /api/fabrics` - 生地一覧取得（フィルター対応）
- [ ] `POST /api/fabrics/{id}/reserve` - 在庫確保
- [ ] 在庫ステータス計算ロジック（既存実装を拡張）

### 6.3 データモデル拡張

- [ ] Fabricモデルに`image_url`追加
- [ ] Taskモデル新規実装

---

## 7. Flutter実装ガイドライン

### 7.1 カラーパレット定義

```dart
class TailorCloudColors {
  static const Color primaryNavy = Color(0xFF000080);
  static const Color textBlack = Color(0xFF111827);
  static const Color successGreen = Color(0xFF10B981);
  static const Color warningYellow = Color(0xFFFBBF24);
  static const Color dangerRed = Color(0xFFEF4444);
  static const Color background = Color(0xFFF8FAFC);
  static const Color border = Color(0xFFE5E7EB);
}
```

### 7.2 コンポーネント

- **KPI Card Widget**: 再利用可能なKPI表示カード
- **Task List Widget**: タスクリスト表示
- **Fabric Grid Widget**: 生地グリッド表示
- **Stock Status Badge**: 在庫ステータスバッジ

---

**最終更新日**: 2025-01  
**デザイナー**: Figma Design Team  
**エンジニア**: AI Assistant (Auto)

